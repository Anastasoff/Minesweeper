namespace Minesweeper.Engine
{
    public enum Commands // inumeration for better identification of commands
    {
        InvalidMove,
        Exit,
        Restart,
        Top,
        ValidMove
    }
}
